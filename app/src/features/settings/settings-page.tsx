import { useState, type FormEvent } from 'react'
import { useAuth } from '@/app/providers/user-provider'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
 export function SettingsPage() { const { changePassword } = useAuth(); const [values, setValues] = useState({ currentPassword: '', password: '', confirmation: '' }); const [message, setMessage] = useState(''); const [error, setError] = useState(''); async function submit(e: FormEvent) { e.preventDefault(); setMessage(''); setError(''); try { await changePassword(values.currentPassword, values.password, values.confirmation); setValues({ currentPassword: '', password: '', confirmation: '' }); setMessage('Password alterada com sucesso.') } catch (cause) { setError(cause instanceof Error ? cause.message : 'Não foi possível alterar a password.') } } return <section className="mx-auto max-w-xl"><h1 className="text-2xl font-semibold">Settings</h1><p className="mt-1 text-sm text-foreground-muted">Change the Password of your account</p><form onSubmit={submit} className="mt-6 space-y-4 rounded-xl border border-border bg-surface p-6"><label className="block text-sm font-medium">Current Password<Input type="password" value={values.currentPassword} onChange={e => setValues(v => ({ ...v, currentPassword: e.target.value }))} required /></label><label className="block text-sm font-medium">New Password<Input type="password" value={values.password} onChange={e => setValues(v => ({ ...v, password: e.target.value }))} minLength={10} required /></label><label className="block text-sm font-medium">Confirm New Password<Input type="password" value={values.confirmation} onChange={e => setValues(v => ({ ...v, confirmation: e.target.value }))} minLength={10} required /></label>{error && <p className="text-sm text-danger">{error}</p>}{message && <p className="text-sm text-success">{message}</p>}<Button type="submit">Change Password</Button></form></section> }
