# pnpm 10+ refuses to run postinstall scripts unless the dependency is
# explicitly allow-listed. The packages allowed here are listed in
# package.json under `pnpm.onlyBuiltDependencies`.
ignore-scripts=false
